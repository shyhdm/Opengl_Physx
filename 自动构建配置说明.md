# 首次配置

本配置尚未替你上传 GitHub，也未改动 D 盘。

## 1. 一次性上传固定依赖包

在本仓库右侧“发行版”点击“创建新版本”：

- 标签（Tag）：`build-deps-v1`，创建这个新标签。
- 目标分支：`main`。
- 标题：`Windows 构建依赖 v1`。
- 上传随本配置提供的 **windows-build-deps.zip**，不要改名或重新压缩。
- 标记为预发布（Pre-release），然后发布。

这是现有 PhysX / Blast / Flow / GLFW / GLM 的头文件、Release 库和运行 DLL，约 136 MB。只需做一次。不要把它提交到源码仓库。

`scripts/dependencies.json` 固定了包的 SHA-256，自动构建会验证。以后只有升级 SDK 才需要重新制作依赖包、改标签与校验值；普通源码更新不需要。

## 2. 合并配置文件到仓库根目录

把配置 ZIP 内的文件合并到：

`D:\Dev\.cpp\Opengl_Physx`

注意是包含 `.git` 和 `Opengl_Physx.slnx` 的外层目录，不是里面那层同名项目目录。

包含：

- `.github/workflows/build.yml`
- `.gitattributes`
- `.gitignore`
- `source-version.txt`
- `启动.bat`
- `scripts/Start.ps1`
- `scripts/Build.ps1`
- `scripts/dependencies.json`
- `Opengl_Physx/CMakeLists.txt`
- `README.md`
- `自动构建配置说明.md`

`.gitignore`、`.gitattributes`、CMakeLists 都基于当前 D 盘文件追加配置，保留本地编译默认路径。**不要手动把 source-version.txt 的占位文本改成某个版本号。**

## 3. 提交并推送 main

打开 GitHub Desktop，提交上述文件并 Push origin。到 GitHub 的 Actions 查看 `Windows runnable build`。

第一次会自动下载 SDK 包、配置 MSVC 14.44、编译、复制 Assets / DLL / VC++ 运行库、压缩并发布对应提交的预发布包。无须配置个人访问令牌，工作流使用 GitHub 自带的临时令牌。

如果仓库禁用了 Actions，需要在 Settings → Actions → General 允许工作流运行；如果组织策略禁止 Release 写入，需要管理员允许工作流的 `contents: write` 权限。

看到绿色成功后，重新通过 **Code → Download ZIP** 下载仓库，解压后双击 `启动.bat` 验证。

## 以后如何更新

正常修改、提交并推送源码即可。每次 main 推送都会自动构建，产生 `build-完整提交号` 对应的程序；不会取消此前提交的构建。启动器只下载这份源码对应的版本，不猜测“最新成功版本”。

ZIP 使用 Git 的 export-subst 写入提交号，用户不需要安装 Git。通过 git clone 获得的仓库使用本地 HEAD；检测到相关源码有未提交修改时会提示本地编译或先提交上传。

自动构建在 GitHub 的 Windows 环境编译，没有 NVIDIA GPU，因此不能代替实际水沙模拟和 GPU 画面测试。

下载者需要兼容显卡和驱动。已将 MSVC 可再发行运行库打入程序包；尚未在没有开发环境的另一台电脑上验证全部功能。

## 本次验证范围

- 使用提取后的固定依赖包，完成本机 MSVC 14.44 Release 编译、链接、资源及运行库复制、ZIP 打包。
- 测试 Git ZIP 提交号展开、首次下载（模拟网络）、同版本离线启动、损坏下载拒绝、版本不符拒绝、无对应构建时不启动旧版。
- GitHub 线上首次构建、真实网络下载和另一台电脑上的运行，需要配置上传后验证。

依赖包来自当前本机 SDK 编译产物；依赖包及程序携带已找到的第三方许可文件。SDK 的修改或更换应重新执行构建和运行验证。
